{
  "name": "stencil-test",
  "scripts": {
    "build": "npm-run-all build:*",
    "build:client": "cd src-stencil && npm run build",
    "build:server": "cd src-tauri && cargo build",
    "dev": "npm-run-all --parallel dev:*",
    "dev:client": "cd src-stencil && npm start",
    "dev:server": "tauri dev",
    "release": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.0.0-rc.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0-rc.8",
    "npm-run-all": "^4.1.5"
  }
}